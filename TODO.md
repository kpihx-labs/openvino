# TODO — openvino

- [ ] First `openvino pull qwen3:1.7b` + `openvino ls` end-to-end test
- [ ] Enrich `search` with offline fallback and `pull --help` examples
- [ ] CI with `make check` (ruff + pyright + pytest)
